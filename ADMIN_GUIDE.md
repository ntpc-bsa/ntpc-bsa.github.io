# 教案後台操作說明（承辦人版）

後台網址：<https://ntpc-bsa.sanity.studio>
用受邀的 Google 帳號登入即可，不需要另外記密碼。

左側是這樣分的：

```
內容
├─ 教案
│   ├─ 全部教案      ← 所有年度混在一起，依學年度排序
│   ├─ 113 學年度    ← 一個學年度一個資料夾
│   └─ 112 學年度
└─ 首頁設定          ← 首頁的焦點新聞
```

**學年度資料夾會自動增加。**只要有一篇教案填了 114 學年度，
左側就會自動多出「114 學年度」，不需要找工程師。
在某個年度資料夾裡按新增，該篇的學年度會自動帶入那一年。

**這個後台只管教案。** 首頁、關於聯盟、活動照片、聯絡我們這幾頁不在這裡，要改請找技術窗口。

---

## 新增一篇教案

左邊選「教案」→ 右上角 **Create**，然後照三個分頁填。

### 基本資料

| 欄位 | 說明 |
|---|---|
| **學年度** | 只填三碼數字，例如 `114`。<br>**填了新的年度，網站選單和該年度的頁面會自動出現，不用找工程師。** |
| **教案標題** | 慣例是「學校 + 科目 + 課程名稱」，例如：`中平國中 健康科 Snow White's Choice` |
| **網址代稱** | 按一下 **Generate** 自動產生就好。<br>已經對外公布過的教案請不要再改，改了舊連結會失效。 |
| **學校** | 學校全銜，例如 `中平國中`。同一所學校每年請填一樣的名稱。 |
| **領域／科目** | 例如 `健康科`、`家政科`、`生活科技` |
| **適用年級** | 例如 `七年級` |
| **授課教師** | 老師姓名 |
| **發布日期** | 首頁「最新消息」會顯示日期最新的 3 篇 |
| **一句話簡介** | 教案卡片上顯示的說明，建議 40～80 字 |
| **標籤** | 例如 `雙語教育`、`健康科`，一個一個按 Enter 新增 |

### 課程內容

| 欄位 | 說明 |
|---|---|
| **課程介紹** | 課程的整體說明，顯示在教案內頁最上方 |
| **教案下載連結** | Google 雲端硬碟的分享連結（見下方注意事項） |
| **下載連結的文字** | 留白就是「教案連結」，想寫「教案 PDF」之類才需要填 |
| **內容段落標題** | 留白就是「課程內容」，通常不用動 |
| **課程內容** | 課堂實施的過程與照片 |

### 封面

**封面圖**是教案列表卡片和首頁會用到的那張，建議選橫式、比例接近 4:3 的照片。

---

## 在文章中間插入照片

在「課程介紹」或「課程內容」的編輯區，把游標移到要插入的位置，
按編輯區的 **+**，選 **圖片組**，然後把照片拖進去。

- 一組放 **1 張** → 整列大圖
- 一組放 **2 張** → 並排顯示

要做「一段文字、一組照片、再一段文字」的效果，就插入多個圖片組。

照片**直接上傳原圖就好**，系統會自動壓縮並轉成適合網頁的格式，不需要自己先縮圖。
每張圖建議填「圖片說明」，這是給視障讀者和搜尋引擎看的。

---

## 發布

填完按右下角 **Publish**。

**按了 Publish 網站不會立刻變**，大約要等 **2～3 分鐘**。
如果超過 10 分鐘還沒更新，請聯絡技術窗口。

存成草稿（不按 Publish）不會出現在網站上，可以安心分次編輯。

---

## 修改首頁的焦點新聞

左側選 **首頁設定**（在「教案（依學年度）」下面），改完一樣按 **Publish**。

| 欄位 | 說明 |
|---|---|
| **焦點新聞圖片** | 建議橫式大圖 |
| **焦點新聞標題** | 例：新北雙語策略聯盟成果展登場 |
| **副標題** | 標題下方的小字，可留白 |
| **內容摘要** | 建議 80～150 字 |
| **「了解更多」連結** | 通常是教育局的新聞稿網址。**留白的話按鈕就不會出現** |
| **按鈕文字** | 留白就是「了解更多」 |

首頁的「最新消息」不用管，它會自動抓發布日期最新的 3 篇教案。

---

## 換活動照片

左側選 **活動照片**，把照片拖進去就好，**拖曳可以調整播放順序**。

每張建議填「圖片說明」（給視障讀者與搜尋引擎看的文字，例如「雙語教學活動」）。
「標題」和「說明文字」是選填的，填了會顯示在照片上，留白就只顯示照片。

原圖直接上傳即可，系統會自動壓縮。

---

## 修改與下架

- **修改**：找到那篇 → 改 → 再按一次 Publish
- **下架**：右下角 **⋮** → **Unpublish**（教案會從網站消失但資料還在，之後可以再發布）
- **永久刪除**：**⋮** → **Delete**（**無法復原**，除非確定不要了否則請用 Unpublish）

---

## 注意事項

**Google 雲端硬碟連結的權限**
貼上連結之前，請先把該檔案的共用權限改成「**知道連結的任何人**可以檢視」。
不然老師點進去會看到「你需要存取權」，這是最常見的問題。

**必填欄位**
學年度、標題、網址代稱、學校、發布日期、一句話簡介、課程介紹、封面圖沒填完，
Publish 按鈕會是停用狀態。畫面上會標示還缺什麼。

**學校名稱要一致**
`樟樹實中` 和 `樟樹國際實中` 會被當成兩所不同的學校，請沿用往年的寫法。

**帳號**
帳號是綁個人的 Google 帳號。承辦人交接時請通知技術窗口新增／移除成員，
不要共用同一組帳號。
